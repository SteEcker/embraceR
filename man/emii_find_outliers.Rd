% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/emii_get_clean_data.R
\name{emii_find_outliers}
\alias{emii_find_outliers}
\title{Identify Extreme Outliers in EMBRACE-II Data}
\usage{
emii_find_outliers(
  data,
  iqr_multiplier = 3,
  save_excel = FALSE,
  excel_path = NULL
)
}
\arguments{
\item{data}{A tibble containing EMBRACE-II data}

\item{iqr_multiplier}{Numeric; multiplier for IQR to define outlier threshold (default: 3)}

\item{save_excel}{Logical; if TRUE, saves the outliers as an Excel file}

\item{excel_path}{Character; path where to save the Excel file (default: NULL)}
}
\value{
A tibble containing outlier information (embrace_id, variable, value, threshold, median)
}
\description{
This function analyzes all numeric columns in the dataset and identifies extreme outliers
using the interquartile range (IQR) method. An extreme outlier is defined as a value that
is more than 3 times the IQR below Q1 or above Q3.
}
\examples{
\dontrun{
  clean_data <- emii_get_clean_data()
  outliers <- emii_find_outliers(clean_data)
  outliers <- emii_find_outliers(clean_data, save_excel = TRUE)
}
}
\keyword{internal}
